The extension interface exports an ``asdf_<extension_name>_copy()`` function containing the necessary machinery to make a deep copy of an object returned by an extension, along with an ``asdf_<extension_name>_copy_into()`` variant that copies into a caller-provided destination (rather than a freshly allocated object).  This is still a work-in-progress--e.g., ``asdf_ndarray_copy`` has not yet been implemented.
